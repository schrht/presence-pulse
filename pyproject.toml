[project]
name = "presence-pulse"
version = "0.1.0"
description = "A lightweight generic utility for minimal periodic user activity."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "pyautogui>=0.9.54",
]

[tool.uv]
package = false
